import "./styles.css";

export const metadata = {
  title: "Instagram · SM Dashboard",
  description: "Manage your Instagram posts, comments, DMs, and insights.",
};

export default function InstagramLayout({ children }: { children: React.ReactNode }) {
  return children;
}
